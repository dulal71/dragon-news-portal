import { montserrat } from '@/app/layout';
import React from 'react';

const SignUp = () => {
    return (
        <div>
            <h1 className={`${montserrat.className}`}>sign up</h1>
        </div>
    );
};

export default SignUp;